# mamba相关的
## [[螺旋mamba(spiral mamba)|螺旋mamba(spiral mamba)]]
<img src="高光谱/mamba相关/螺旋mamba_fusion用了/屏幕截图 2025-09-24 194906.png" width="400px">
### [[特征融合|Fusion]]
- 三个步骤:关系建模,权重增强,特征融合
## [[Dualmamba|Dualmamba(双分支mamba)]]
<img src="高光谱/mamba相关/DualMamba_一种用于高光谱图像分类的轻量级光谱空间mamba卷积网络/屏幕截图 2025-08-28 155315.png" width="800px">
### 交叉注意谱空间Mamba模块(CAS 2 MM)
- 在该模块中,设计了动态位置嵌入(DPE)增强视觉序列的空间位置信息
### 轻量级的谱空Mamba块
- 1.有效的扫描策略
- 2.轻量级的Mamba设计
- 3.交叉谱空融合(CAS 2F)
### 自适应全局-局部融合方法
![[自适应融合.png]]



## [[研究课题/01_高光谱分类/Mamba系/MambaHSI/note1|MambaHSI]]
![[Mambahsi.png]]
- 空间Mamba块,利用Mamba长序列建模能力
- 频谱Mamba块,对通道分组后送入Mamba考察各个组之间的关系
- 自适应融合
## [[HSI-MFormer|HSI-Former]]
- <img src="HSI-Mformer.png" width = "800px">
### 1.MTG(多尺度令牌生成模块)
- 使用不同内核尺寸的3d卷积,这些卷积随后被传输到mamba和transformer中进行短程和远程空间光谱特征提取
- 如图MTG由三个卷积组构成,这个生成HSI块的过程可表述为
$$
F_{s} = ReLU(BN(3DConv_{s\times s\times s(x)}))
$$
$$
F_{m} = ReLU(BN(3DConv_{m\times m\times m(x)}))
$$
$$
F_{l} = ReLU(BN(3DConv_{l\times l\times l(x)}))
$$
- 三个尺度的滤波器个数都是32
- 在每个卷积组后,MTG进一步设计一个用于维度转换的线性嵌入层,以s为例
$$
T_{s} = Linear(f_s)
$$
- 就这样,生成了三个频谱-空间标记组$T_s,T_m,T_l$,嵌入操作对每个组独立进行,没有参数共享
### 2.ITE(内尺度transformer专家)
- ITE将三个尺度的token传入三个MHSA中进行短程依赖建模随后配合残差连接,以s组为例,可表示为
$$
F_{s} = T_s + MHSA_{s}(LN(T_{s}))
$$
- 之后,将所有组得到的特征链接起来全面利用获得的信息
$$
F_{ITE} = Concat(F_{s},F_m,F_l)
$$
- 随后,通过一个FFN模块(两个线性层和一个激活函数组成的前馈网络)进行非线性变化,增强模型的表示能力
$$F_{ITE} = F_{ITE}+FFN(F_{ITE})$$
### 3.跨尺度Mamba专家(CME)
![[屏幕截图 2025-10-16 224412.png]]
- 首先,CME将所有标记组按分辨率顺序集成并展平为一个长序列,考虑正向和反向扫描方向以增强对空间的感知能力,以MTG生成的标记组为输入,展平过程可表示为:
$$
S_{forward} = \left[T_{S}^1\cdots T_{S}^{S^2},T_{M}^1\cdots T_{M}^{M^2},T_{L}^1\cdots T_{L}^{L^2} \right]
$$
$$S_{backward}为S_{forward}的反向
$$
- 随后,展平的序列被传输进双向mamba块进行独立的远程建模,对于每个方向,序列首先经过归一化,随后传入两个并行的线性嵌入层
- 其中一个分支z经过一个激活函数生成门控信号
- 另一个分支经过一个1d卷积再经过激活函数后送入S6模型,随后进行层归一化和门控操作,最后,特征被输入到带有残差连接的线性层
$$z = \sigma(Linear(Norm(S)))$$
$$f = \sigma(Conv(Linear(S)))$$
$$F = Linear(Norm(S6(f)) \times z ) + S$$
- 这里和z的操作是矩阵乘法

- 最终,经过并行处理后,生成的特征分为$F_{forward}$和$F_{backward}$,随后通过向量加法进行聚合,得到输出结果
$$F_{MCE} = F_{forward} + F_{backward}$$


## [[E-mamba|E-mamba]]
#对C矩阵的交换
![[e_mamba.png]]
### 孪生视觉状态空间块(SVSSB)
![[屏幕截图 2025-11-06 152816.png]]
- 首先,输入高光谱和雷达数据$X_{lidar}\in\mathbb{R}^{h\times w\times1}$
- 2.通过$1\times1$卷积将单通道数据和多通道数据对齐(高光谱降维,雷达升维)
- 3.以真值像素为中心构建图像立方体$x_{m}^{(i,j)}\in\mathbb{R}^{C\times p\times p}$
- 4.四方向交叉扫描(两种左上到右下,两种右下到左上)
- 5.将每个方向的扫描结果馈送到SSM4个中,将4个输出进行一个Merge(融合)的操作,形成一个2d特征图,这个特征图包含了每个像素周围方向的内容
### 跨mamba特征提取分支(CMFEB)
![[屏幕截图 2025-11-05 230236.png]]
- CMFEB采用两个输入要素集并产生两个输出,同时保持输入要素的原始形状
- 首先,通过线性层和[[卷积这一块|深度卷积(DWConv)]]处理两个输入要素,然后传入CSSM模块
- 用线性投影层构建B,C$\Delta$等矩阵,作为SSM的矩阵
- **寻求促进不同SSM的信息交换(如下) **
![[屏幕截图 2025-11-05 232251.png]]
即使用hsi生成的C解码lidar的内容

### 模态引导的mamba融合模块
- CMFEB接受来自两个异构数据源的输入并输出特征,提出模态引导的mamba融合模块整合来自两个模态的关键信息
- Transformer受限于二次尺度约束Token的大小有限,利用mamba的高效长序列建模能力,MMFB直接处理连接的特征
- 来自CMFEB的两个输出$F_{hsi}^k$和$F_{lidar}^k\in\mathbb{R}^(H_{w}\times W_{W}\times k)$先被输入线性层和卷积层然后馈送到CSSM(Concat selective scan model)中,然后展平为$\mathbb{R}^{((H_{w}\times W_{W})\times k)}$,然后两个展平的特征图被链接在一起形成$S_{concat}^k\mathbb{R}^{((2\times H_{w}\times W_{W})\times k)}$,
- 为了获得全面的全局信息,将内容做一个反转,将两个图分别馈送到SSM中
- 逆序列随后被反转回来,与原图像相结合,最后将结合的图分离分别翻转为原先的特征图形状


## [[MambaHSI+|mambahsi+]]
### 主要创新
### 1.双向上下文建模模块（Reverse Bidirectional Contextual Modeling, RBCM）
 - **作用:通过双向重构输入张量,将水平和垂直空间依赖性编码为像素级特征,同时保留空间拓扑.逆序增强了邻域交互,而不牺牲空间粒度,克服了传统方法的局部上下文限制**
### 2.光谱轨迹学习（Spectral Trajectory Learning, STL)
- **作用:确保每个波段的最终表示全面融合了所有光谱波段的信息**
### 3.Mamba增强通道注意模块
- **作用:将光谱通道与序列建模要求对齐,同时保留空间光谱的拓扑关系,从而实现c个谱带的有效复杂度**
 ![[mambahsi+.png]]A.动机
### B.体系综合建构
#### 1.双向状态空间建模
- 开发了一个双路径空间建模架构,其中隐藏状态在光栅扫描和反向方向上传播,捕获全局空间上下文
#### 2.光谱学习轨迹
- 频谱维度被建模为连续状态轨迹,每个频带的隐藏状态$s_\lambda$通过动态学习矩阵$A_\lambda$演变,过滤噪声的同时保持频带间相关性
#### 3.自适应特征融合
空间和频谱的融合

### C.反向双向上下文建模
- 图二(b),结合双向传播,反向排序和自适应融合,通过双向重构输入张量,将水平和垂直空间依赖性编码为像素级特征,同时保留空间拓扑
- 逆序增强了邻域交互,而不牺牲空间粒度,克服了传统方法的局部上下文限制
- 令$x\in \mathbb{R}^{b\times c\times h\times w}$表示输入张量,$X_{ij}$表示空间位置为i,j的特征,为了捕获双向空间依赖性,沿着行和列执行状态空间传播
![[屏幕截图 2025-10-10 150104.png]]
- 这段公式表示了行方向和列方向的传播过程,简单来讲,行方向上有两个方向的传播,传播后经过给予注意力的加权求和获得一个隐藏状态$h_{ij}^{r}$,同样的,列方向上也生成一个隐藏状态$h_{ij}^c$
- 这种传播机制被设计为递归的沿着前向和反向聚集沿着行或列的所有先前和后续特征,从而确保得到的所有隐藏状态是对应行和列的输入特征的单射函数
- 执行逆序序列增强后,处理的序列被整理会原始的2d布局,最终,行和列的布局的自适应特征如下:

$$h_{i,j} = \gamma_{i,j} \odot h^{(r)}_{i,j} + \left(1 - \gamma_{i,j}\right) \odot h^{(c)}_{i,j}$$
其中有:

$$\gamma_{i,j} = \text{softmax}\left( W_\gamma \cdot \left[ h^{(r)}_{i,j}; h^{(c)}_{i,j} \right] \right)$$
- **作用:捕获全局和局部空间依赖性,同时对输入的2d结构进行唯一编码**
### D.光谱轨迹学习
- **目的:补充空间建模**
- 将光谱维度建模为连续状态轨迹,聚合来自所有光谱带的信息沿着光谱维度,定义两个状态:前向状态(forward state)和翻转状态(flipped state),与MambaHSI的单向传播相比,引入双向光谱状态传播机制,确保每个波段的最终表示全面融合了所有光谱波段的信息
- 如下是前向光谱和反向光谱状态的递归地定义方式
![[屏幕截图 2025-10-10 154059.png]]
- A是可学习的动力学矩阵,控制光谱状态的演变,B对输入x进行加权
#### 动态加权
- **目的:增强动态矩阵A的灵活性和表示性能
- 过程:使用对数空间设置初始化A,然后应用可学习的线性投影动态生成$A_{\lambda}$
$$\mathbf{A}\lambda = -\exp\left( \mathbf{W}_{\text{proj}} \cdot \log(\mathbf{A}_0) \right) + \mathbf{A}_0$$
- $A_0$是具有集合间隔元素的初始矩阵,$W_{proj}$是可学习的线性投影矩阵
- **在最后一个光谱带和第一个链接起来,学习更丰富的光谱特征**
### fusion
- 为了整合两个轨迹并捕获带间相关性,最终光谱特征被定义为来自所有带的贡献的加权和
![[屏幕截图 2025-10-10 191456.png]]
其中的$W_s$是一个可学习的参数矩阵,分号表示向量级联,softmax将权重归一化为k确保融合是整个上下文的单射映射
- **作用:该公式确保每个谱带的最终表示是所有输入特征{$x_{k}$}$_{k=1}^C$的唯一函数,从而在衰减谱噪声的同时保留固有的带间相关性
### Mamba增强通道注意模块

- **作用:将光谱通道与序列建模要求对齐,同时保留空间光谱的拓扑关系,从而实现c个谱带的有效复杂度**
- 首先通过自适应平均池化聚合全局空间信息
$$F_{g}=AdaptiveAvgPool2d(x)\in\mathbb{R}^{b\times c\times 1\times 1}$$
- 为了便于谱序列建模,通过置换和挤压操作重塑池化特征
$$
F_{t} = Permute(Squeeze(f_{g})) \in \mathbb{R}^{b\times 1\times c}
$$
- 核心创新在于用基于Mamba的空间状态模型替代传统的一维卷积,该模型通过隐藏状态捕获长程谱相关性
$$
h'(t) = Ah(t)+Bx(t)
$$
$$
F_{m} = SSM_{\theta}(F_{t}) + Conv1D(F_{t})
$$
- 其中$SSM\theta$表示可学习的状态空间投影,生成的特征经过激活生成通道注意力权重
$$
\mathbf{W}_c = \sigma\left( \text{Unsqueeze}\left( \text{Permute}\left( \mathbf{F}_m \right) \right) \right) \in \mathbb{R}^{B \times C \times 1 \times 1}
$$
最终,通过逐元素乘法将得到的权重与原特征图相乘


## [[Hyperspectal Image Classification With Mamba|HICWM]]

![[屏幕截图 2025-09-11 172127.png]]<br>
Mamba采用双分支架构,spaM和speM是其核心,
### spaM
![[屏幕截图 2025-09-11 174112.png]]
- 包括多尺度空间信息提取模块,局部空间相关提取模块和全局空间上下文建模模块
### speM
![[屏幕截图 2025-11-11 234923.png]]
- 包括多尺度光谱信息提取模块,光谱提取模块和SDCC模块

- 设$f \in \mathbb{R}^{H\times W\times C}$为输入HSI,HWC分别表示行数,列数和谱带数,首先,利用$1\times 1$二维卷积,批归一化和高斯误差线性单元提取浅层特征,该过程可表示为$F_{s}= In\_head(F)$
#### spam
- 在spaM中,先利用MSFE整合不同局部空间尺度的信息,在此基础上利用LSCE方法从特征中识别出局部空间相关性,然后利用跳跃链接将多尺度空间信息和局部空间相关性结合起来
$$
F_{ms}^{spa} = MSFE_{spa}(F_s)
$$
$$
F_{spa}^l = LSCE(F_{ms}^{spa}) + F_{ms}^{spa}
$$
- GSCE用于提取全局语义,GSCE的输出$F_{spa} \in\mathbb{R}^{H\times W\times C}$可以如下获得
$$F_{spa} = GSCE(F_{spa}^l)$$
#### spem
- 在speM中,首先使用MSFE进行多尺度光谱信息的初始建模和集成,然后使用光谱分组模块进行光谱信息的粗聚类
$$
F_{ms}^{spe} = MSFE_{spe}({F_s})
$$ $$
X= Group_{m,n}(F_{ms}^{spe})
$$
- 其中,$Group_{m,n}$表示频谱分组模块,m是每个频带组中频带的数量,n是介于两个相邻频带组的起始频带之间的频带的数量
- $X = (x_1 , x_2, x_3\cdots x_g)^T \in\mathbb{R}^{g\times m}$为波段组序列,利用mamba对序列中的光谱信息进行详细分析,旨在对全局频谱信息沿着信道轴的动态变化进行建模
$$
F_{spe} = SDCC(X)
$$
其中,$F_{spe} = (w_1, w_2, w_3, \cdots w_c)\in\mathbb{R}^{1\times1\times c}$表示SDCC的处理结果,随后,通过一个通道级乘法运算,实现了通道级光谱和空间信息的耦合
$$
F_{fusion} = F_{spa}\odot F_{spe}
$$
- 用spe的输出充当$F_{spe}$的权重,从而允许光谱分支增强空间分支
- 最终,应用一个输出模块,该模块用一个$3\times3$的平均池化层组成,然后进行通道卷积,然后应用argmax函数确定图像中每个像素的类别
$$
F_{out} = Con_{1\times1}(Avgpool_{3\times3}(F_{fusion})
$$
$$
Y = argmax(F_{out})
$$
- 其中,$F_{out} = (p_1, p_2, p_3,\cdots p_k)\in\mathbb{R}^{H\times W\times K}$表示相应位置的像素属于第k类的概率
,$Y\in\mathbb{R}^{H\times W}$表示最终输出图
# 其他
## [[CLOLS|CLOLN]] 
<img src="高光谱/CLOLN/屏幕截图 2025-10-23 152814.png" width="400px">
### 优点1:$COS^2M$轻量化:需要的参数极少
- 低输出通道的逐点卷积(P-Conv)
- 空间分支:$c_{in}\times k_{w}\times k_{h} \times k_{d}\times C_{out}$
- 光谱分支:深度可分离卷积每个卷积对一个光谱波段进行扫描
### 优点2:邻域像素感知激活函数
- 对邻域做激活函数后加权求和作为激活结果,增强非线性表达能力
- 权重可学习,且计算量略优于2d卷积
## [[分析_SSFTTnet|SSFTT]]
<img src="高光谱/SSFTT_用于高光谱图像分类的光谱-空间特征令牌化/SSFTT.png" width = "400px">
- 简单的2d和3d卷积层提取浅层特征
- 高斯特征加权令牌化
- transformer的encoder和卷积的结合

## [[note|EATN:自相似特征增强]]
<img src = "EATN.png" width = 600px>

<img src = "GISSA.png" width=600px>


- 关键组件:SSFE,SCA,SIT<br>
1. 假设输入HSI可以表示为$P\in\mathbb{R} ^{H\times W\times C}$其中,HW分别表示长度和宽度,C表示波段数,

2. 将选定的像素和其相邻像素作为patch输入网络,补丁可以表示为$P\in\mathbb{R}^{S\times S\times C}$其中$S\times S$表示patch的大小,SSFE模块生成两个大小相同的自相似性描述符,然后将他们与原始补丁融合,特征图的大小保持为$S\times S\times C$

3. 将特征图输入到空间和光谱特征提取模块:
- 对于光谱分支,使用$1\times1$的2D卷积来降低特征图的维度,随后,将其输入到提出的[SIT模块](#SIT)
- 对于空间分支,采用核尺寸为$3\times3$的二维卷积对特征图进行降维,随后将其送入一系列[SCA模块](#SCA)中
4. 降维后空间分支和光谱分支的维数为$S\times S\times D$,此外,初始化一个可训练的加权参数$\lambda $,并对特征图进行加权求和

5. 最后,通过结合一个dropout层和一个全局平均池化构造一个线性分类器,得出最终的分类结果
## [[DSFormer|DSFormer]]

### 总体概述
- ![[DSFormer.png]]


- 先进行PCA降维,然后像素中心提取彼此重叠的patch立方体,$\{x_{i}\in \mathbb{R}^{c \times w \times w}\}^{H\times W}$,其中,H表示原图像高,W表示原图宽
- 采用一个$3\times{3}$卷积提取浅层特征表示为$F_{1}$,随后用两个连续的DSFTG块对$F_{1}$进行深层的特征提取,这个过程可以表示如下
$$
F_{2} = DSFTG_{2}(DSFTG_{1}(F_{1}))
$$
- 最终,在分类头应用层归一化,然后应用全连接层获得最终的分类结果
#### KSFA

-  ![[KSFA.png]]
- 可分离深度卷积
- 平均池化,全局池化后融合
#### TSFA
- ![[TSFA.png]]
- top-k-token

##